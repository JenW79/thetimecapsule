name: required-sync-check

on:
  pull_request:
    branches: [development]

jobs:
  validate-sync:
    name: required-sync-check
    runs-on: ubuntu-latest
    steps:
      - name: Confirm sync for branch protection
        run: echo "✅ This check ensures the branch is synced with development."

