name: sync-check

on:
  pull_request:
    branches: [development]

jobs:
  confirm-up-to-date:
    name: Check branch is up-to-date
    runs-on: ubuntu-latest
    steps:
      - name: Print confirmation
        run: echo "Safe Sync Check passed!!!"
